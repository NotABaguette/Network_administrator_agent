# Everything the guests need from Ansible: the two exporters.
#
#   ansible-playbook -i inventory/guests.yml playbooks/site.yml
#
# The inventory is generated by `infra onboard seed-guests`; credentials are
# supplied on the command line (see ../README.md).
---
- import_playbook: node_exporter.yml
- import_playbook: windows_exporter.yml
