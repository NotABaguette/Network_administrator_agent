[defaults]
inventory = inventory/guests.yml
host_key_checking = True
retry_files_enabled = False
stdout_callback = yaml
# Secrets never live in this repository: pass --user/--private-key or use your
# own vault. See README.md.
nocows = True

[ssh_connection]
pipelining = True
